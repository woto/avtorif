<h1>New job</h1>

<% semantic_form_for(@job) do |f| %>
  <%= f.error_messages %>

    <%= f.input :jobable, :polymorphic => [ :user, :group, :some_other_model ] %>

    <%= f.inputs %>


  <p>
    <%= f.buttons %>
  </p>
<% end %>

<%= link_to 'Back', jobs_path %>