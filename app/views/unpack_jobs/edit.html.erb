<h1>Editing unpack_job</h1>

<% semantic_form_for(@unpack_job, :url => supplier_job_unpack_job_path(params[:supplier_id], params[:job_id], params[:id])) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

<%= link_to 'Back', supplier_jobs_path(params[:supplier_id]) %>
