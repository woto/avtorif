<h1>Editing filter_job</h1>

<% semantic_form_for(@filter_job, :url => supplier_job_filter_job_path(params[:supplier_id], params[:job_id], params[:id])) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

<%= link_to 'Back', supplier_jobs_path(params[:supplier_id]) %>