<h1>New convert_job</h1>

<% semantic_form_for(@convert_job, :url => job_convert_jobs_path(params[:job_id])) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', convert_jobs_path %>