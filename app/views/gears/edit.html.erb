<h1>Editing gear</h1>

<% form_for(@gear) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @gear %> |
<%= link_to 'Back', gears_path %>